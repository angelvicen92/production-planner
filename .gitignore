node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.cache
.local
.upm
# V4 benchmark runtime output
engine/v4/benchmarks/latest-result.json

local_engine_scenarios/

# Historical ORC artifacts are not part of the current operational state.
# Failed artifacts are kept locally for diagnostics only.
# Only the latest accepted Planner Next artifact is versioned.
/plan-27-orc-*.json
/planner-next-*.failed.json
