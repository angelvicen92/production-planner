import test from "node:test"; import assert from "node:assert/strict";
import { buildInitialConstructionMap } from "./initialConstructionMap";
const state:any={id:"s",planning:[],locks:[]}; const base:any={workDay:{start:"08:00",end:"12:00"},tasks:[]};
test("map omits contestant zero and separates space and zone pressure",()=>{ const input:any={...base,spaces:[{id:1}],zones:[{id:1}],spaceCapacityById:{1:1},maxTemplateChangesByZoneId:{1:2},tasks:[{id:1,templateId:1,contestantId:null,spaceId:1,zoneId:1,durationMin:30,status:"pending"},{id:2,templateId:2,contestantId:2,spaceId:2,zoneId:1,durationMin:20,status:"pending"}]}; const m:any=buildInitialConstructionMap({input,state,planningMode:"INITIAL_CONSTRUCTION"}); assert.equal(m.contestantPressure.some((c:any)=>c.contestantId===0),false); assert.equal(m.spacePressure.find((s:any)=>s.spaceId===1).pendingTaskCount,1); assert.equal(m.zonePressure.find((z:any)=>z.zoneId===1).pendingTaskCount,2); assert.equal(m.identityEvidence.spaceZoneIdentitySeparated,true); });
test("map classifies synthetic main-flow downtime as protected not anchor eligible and critical path is longest branch",()=>{ const input:any={...base,spaceCapacityById:{7:1},tasks:[{id:-1,templateId:9,spaceId:7,durationMin:20,status:"pending",type:"meal"},{id:1,templateId:1,contestantId:1,spaceId:7,durationMin:30,status:"pending"},{id:2,templateId:2,contestantId:1,durationMin:40,status:"pending",dependsOnTaskId:1},{id:3,templateId:3,contestantId:1,durationMin:50,status:"pending",dependsOnTaskId:1}],optimizerMainZoneId:7}; const m:any=buildInitialConstructionMap({input,state,planningMode:"INITIAL_CONSTRUCTION"}); assert.equal(m.mainFlowPressure.mainFlowProductiveTaskIds.includes(-1),false); assert.equal(m.mainFlowPressure.mainFlowProtectedDowntimeTaskIds.includes(-1),true); const n=m.dependencyGraph.nodes.find((n:any)=>n.taskId===1); assert.equal(n.downstreamCriticalPathMinutes,50); });
