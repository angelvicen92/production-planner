import test from "node:test"; import assert from "node:assert/strict";
import { buildInitialConstructionResidualContext } from "./initialConstructionResidualContext";
const state:any={id:"s",planning:[],locks:[]};
test("residual context is immutable, excludes provisional work but preserves graph/status",()=>{ const input:any={workDay:{start:"08:00",end:"12:00"},tasks:[{id:1,durationMin:10,status:"pending"},{id:2,durationMin:10,status:"pending"},{id:3,durationMin:10,status:"pending"},{id:4,durationMin:10,status:"pending",dependsOnTaskId:2}]}; const before=JSON.stringify(input); const c:any=buildInitialConstructionResidualContext({originInput:input,originOperationalState:state,stage2:{selectedPartialPlanId:"pp",selectedBranchId:"b",selectedAnchorTaskId:3,closureTaskIds:[1,2,3],selectedAssignments:[{taskId:1,startPlanned:"08:00",endPlanned:"08:10"},{taskId:2,startPlanned:"08:10",endPlanned:"08:20"},{taskId:3,startPlanned:"08:20",endPlanned:"08:30"}]}}); assert.deepEqual(c.provisionallyAssignedTaskIds,[1,2,3]); assert.equal(c.residualPendingTaskIds.includes(4),true); assert.equal(input.tasks.find((t:any)=>t.id===2).status,"pending"); assert.equal(JSON.stringify(input),before); assert.throws(()=>((c as any).readOnly=false)); });
