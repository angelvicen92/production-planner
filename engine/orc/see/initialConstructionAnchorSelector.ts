import type { EngineInput } from "../../types";
import { deepFreeze } from "../immutability";
import { resolveORCPlanningEntryOperationalRoleMetadata } from "../state/nonWorkTaskClassifier";
import { resolveORCTransportContract } from "../state/transportContractResolver";
import { taskPrerequisiteIds } from "../understanding/initialConstructionMap";
const dur=(t:any)=>Number(t.durationOverrideMin??t.durationMin??t.durationMinutes??t.duration??0)||0;
export function selectInitialConstructionAnchors(args:{input:EngineInput; initialConstructionMap:any; maxAnchors?:number}){ const {input,initialConstructionMap:map}=args; const max=args.maxAnchors??12; const tasks=[...(input.tasks??[])].sort((a,b)=>a.id-b.id); const invalid=new Set((map.blockers??[]).map((b:any)=>b.taskId)); const transport=resolveORCTransportContract(input as any); const meal=(input.actualMeal??input.mealWindow??input.meal) as any; const cById=new Map((map.contestantPressure??[]).map((c:any)=>[c.contestantId,c])); const nodeById=new Map((map.dependencyGraph?.nodes??[]).map((n:any)=>[n.taskId,n])); const regions=map.bottleneckRegions??[]; const anchors=tasks.filter(t=>t.status!=="done"&&t.status!=="in_progress"&&!invalid.has(t.id)).filter(t=>resolveORCPlanningEntryOperationalRoleMetadata({task:t,mealWindow:meal,transportContract:transport}).countsAsWork).map(t=>{ const node:any=nodeById.get(t.id)??{}; const cp:any=cById.get(t.contestantId as any)??{}; const main=(map.mainFlowPressure?.mainFlowTaskIds??[]).includes(t.id); const region=regions.find((r:any)=>r.relatedTaskIds?.includes(t.id))??regions[0]??null; const resourceScarcity=Math.max(0,...(map.resourcePressure??[]).filter((r:any)=>r.relatedTaskIds?.includes?.(t.id) || true).map((r:any)=>Number(r.scarcity)||0)); const structural=(map.spacePressure??[]).find((s:any)=>s.spaceId===t.spaceId||s.spaceId===t.zoneId)?.structuralSlack; const hardRisk=t.fixedWindowStart||t.fixedWindowEnd?1:0; const priority=[hardRisk, cp.slackMinutes==null?0:1, cp.slackMinutes==null?999999:cp.slackMinutes, -Number(cp.workloadRatio??0), -Number(node.chainDurationMinutes??dur(t)), main?0:1, -resourceScarcity, structural==null?0:Math.min(0,structural), -Number(node.unlockCount??0), t.id]; return {anchorTaskId:t.id,bottleneckRegionId:region?.id??null,contestantId:t.contestantId??null,spaceId:t.spaceId??null,zoneId:t.zoneId??null,mainFlow:main,directPrerequisiteTaskIds:node.directPrerequisiteTaskIds??taskPrerequisiteIds(t,tasks),transitivePrerequisiteTaskIds:node.transitivePrerequisiteTaskIds??[],directDependentTaskIds:node.directDependentTaskIds??[],chainDurationMinutes:node.chainDurationMinutes??dur(t),contestantSlackMinutes:cp.slackMinutes??null,contestantWorkloadRatio:cp.workloadRatio??null,resourceScarcity,structuralPressure:structural==null?null:-structural,futureFreedomRisk:(cp.slackMinutes??999999)<60 || (node.unlockCount??0)>0,lexicographicPriority:priority,selectionReason:`Anchor ${t.id}: ${main?"main-flow; ":""}slack=${cp.slackMinutes??"unknown"}; chain=${node.chainDurationMinutes??dur(t)}; unlock=${node.unlockCount??0}.`,readOnly:true}; }).sort((a,b)=>{ const A=a.lexicographicPriority, B=b.lexicographicPriority; for(let i=0;i<Math.max(A.length,B.length);i++){ if(A[i]!==B[i]) return A[i]<B[i]?-1:1; } return a.anchorTaskId-b.anchorTaskId; }).slice(0,max); return deepFreeze(anchors) as any; }
