import test from "node:test"; import assert from "node:assert/strict"; import type { EngineInput } from "../../types"; import { buildORCInitialConstructionInput } from "./buildORCInitialConstructionInput";
const input=():EngineInput=>({planId:1,workDay:{start:"08:00",end:"18:00"},meal:{start:"13:00",end:"14:00"},camerasAvailable:1,tasks:[{id:1,planId:1,templateId:10,status:"pending",durationOverrideMin:30,spaceId:5,dependsOnTaskIds:[2],resourceRequirements:{byItem:{7:1},anyOf:[{quantity:1,resourceItemIds:[8,9]}]},startPlanned:"09:00",endPlanned:"09:30",assignedResourceIds:[7],seedSource:"v4_planned_task"},{id:2,planId:1,templateId:11,status:"done",durationOverrideMin:20,startPlanned:"08:00",endPlanned:"08:20",assignedResourceIds:[8]}],locks:[],zoneResourceAssignments:{},spaceResourceAssignments:{},zoneResourceTypeRequirements:{},spaceResourceTypeRequirements:{},planResourceItems:[],resourceItemComponents:{},groupingZoneIds:[]});
test("clears inherited pending planning while preserving operational identity",()=>{ const r=buildORCInitialConstructionInput(input()); const t:any=r.input.tasks[0]; assert.equal(t.startPlanned,undefined); assert.equal(t.endPlanned,undefined); assert.equal(t.assignedResourceIds,undefined); assert.equal(t.spaceId,5); assert.deepEqual(t.dependsOnTaskIds,[2]); assert.deepEqual(t.resourceRequirements?.byItem,{7:1}); assert.equal(r.diagnostics.v4SeedUsed,false); assert.equal(r.diagnostics.operationalIdentityPreserved,true); });
test("preserves done in-progress and locked planning",()=>{ const r=buildORCInitialConstructionInput(input()); assert.equal(r.input.tasks[1].startPlanned,"08:00"); assert.deepEqual(r.input.tasks[1].assignedResourceIds,[8]); });
