import { createHash } from "node:crypto";
import type { EngineInput } from "../../types";
import type { OperationalState, ReasoningBudgetProfile } from "../contracts";
import { deepFreeze } from "../immutability";
import { stableStringify } from "../structuralEquality";
import { buildInitialConstructionMap } from "../understanding/initialConstructionMap";
import { buildInitialConstructionResidualContext } from "../understanding/initialConstructionResidualContext";
import { buildInitialConstructionSearchSpaces } from "../see/initialConstructionSearchSpace";
import { buildCandidateStates } from "../transformation/transformationEngine";
import { simulateCandidateStates } from "../simulation/simulationEngine";
import { validateSimulatedStates } from "../validation/validationEngine";
import { materializeInitialConstructionAnchorAttempt, initialConstructionAssignmentFingerprint } from "./materializeInitialConstructionAnchorAttempt";
import { buildInitialConstructionFrontierFailureEvidence } from "./conflictDirectedInitialConstructionBackjump";
import { evaluateInitialConstructionPartialPlanFutureFeasibility, type InitialConstructionCriticalChainPartialPlan } from "./initialConstructionPartialPlanFutureFeasibility";
import type { InitialConstructionCanonicalContext } from "../understanding/initialConstructionCanonicalContext";
import { selectInitialConstructionAnchors } from "../see/initialConstructionAnchorSelector";
import { buildInitialConstructionExecutableFrontierPortfolio } from "../understanding/initialConstructionExecutableFrontierPortfolio";

const digest=(value:any)=>createHash("sha256").update(stableStringify(value)).digest("hex");
const normalize=(a:any)=>({taskId:Number(a.taskId),startPlanned:a.startPlanned??null,endPlanned:a.endPlanned??null,spaceId:a.spaceId??null,resourceIds:[...(a.resourceIds??a.assignedResourceIds??[])].map(Number).sort((x,y)=>x-y)});
const candidate=(id:string,assignments:any[])=>({id,assignments,state:{status:"draft",evidenceIds:[],metadata:{readOnly:true}},metadata:{strategy:"SCHEDULE_PENDING_TASKS",planningInfluence:"candidate-assignments",initialConstructionStage:"retained-frontier",executesTransformations:true,commitsPlanning:false,readOnly:true},evidenceIds:[],operationalValues:[]}) as any;

export interface ExpandInitialConstructionPartialPlanOnceBudget { maxCriticalChainsPerDecision?:number; maxExecutableFrontierTasksPerChain?:number; maxRetainedChainBranches?:number; maxBranchEvaluationsPerFrontierTask?:number; maxBranchEvaluationsPerAnchor?:number; maxRetainedValidBranchesPerFrontierTask?:number; initialCriticalChainBatchSize?:number; maxCriticalChainRanksScannedPerExpansion?:number; initialFrontierTaskBatchSize?:number; initialExecutableFrontierBatchSize?:number; maxFrontierTasksScannedPerChain?:number; maxExecutableFrontierTasksScannedPerExpansion?:number; maxChildrenPerDecision:number }

/** Expands one immutable node using deterministic progressive widening across
 * chain ranks, frontier tasks, and raw branch evaluations before declaring a
 * true dead end. */
export function expandInitialConstructionPartialPlanOnce(args:{originInput:EngineInput;originOperationalState:OperationalState;stage1:any;canonicalContext:InitialConstructionCanonicalContext;parentPartialPlan:InitialConstructionCriticalChainPartialPlan;budget:ExpandInitialConstructionPartialPlanOnceBudget;caches?:{seenAssignmentsFingerprints?:Set<string>};createdOrdinal:number;reasoningBudget?:ReasoningBudgetProfile|null;createdAt?:string|null;causalReopenContext?:{excludedDecisionBranchFingerprints?:readonly string[];excludedAssignmentsFingerprints?:readonly string[];previouslyEvaluatedTemporalCandidateFingerprints?:readonly string[];targetPrimaryGoalTaskId?:number|null;targetExecutionTaskId?:number|null;readOnly:true}|null}){
 const parentAssignments=[...args.parentPartialPlan.assignments].map(normalize).sort((a,b)=>a.taskId-b.taskId);
 const pseudo={selectedAssignments:parentAssignments,selectedPartialPlanId:args.parentPartialPlan.partialPlanId,selectedValidationResult:"VALID"};
 const residualContext=buildInitialConstructionResidualContext({originInput:args.originInput,originOperationalState:args.originOperationalState,stage2:pseudo});
 const map=buildInitialConstructionMap({input:args.originInput,state:args.originOperationalState,planningMode:"INITIAL_CONSTRUCTION",provisionalAssignments:parentAssignments,provisionallyAssignedTaskIds:parentAssignments.map(a=>a.taskId)});
 const canonicalAnchors:any[]=selectInitialConstructionAnchors({input:args.originInput,initialConstructionMap:map,maxAnchors:Number.MAX_SAFE_INTEGER});
 const anchorsByTaskId=new Map(canonicalAnchors.map(anchor=>[Number(anchor.anchorTaskId),anchor]));
 const allChains=[...(map.criticalChains??[])];
 const portfolio=buildInitialConstructionExecutableFrontierPortfolio({parentAssignmentsFingerprint:args.parentPartialPlan.assignmentsFingerprint,initialConstructionMap:map,canonicalAnchors});
 const frontierBatch=Math.max(1,Number(args.budget.initialExecutableFrontierBatchSize??args.budget.initialFrontierTaskBatchSize??4));
 const frontierLimit=Math.max(frontierBatch,Number(args.budget.maxExecutableFrontierTasksScannedPerExpansion??args.budget.maxFrontierTasksScannedPerChain??32));
 const explorationLimit=Math.max(1,Number(args.budget.maxBranchEvaluationsPerFrontierTask??args.budget.maxBranchEvaluationsPerAnchor??48));
 const retainLimit=Math.max(1,Number(args.budget.maxRetainedValidBranchesPerFrontierTask??args.budget.maxRetainedChainBranches??3));
 const children:any[]=[], hardInvalid:any[]=[], futureInfeasible:any[]=[], duplicates:any[]=[], frontierAudits:any[]=[], frontierFailureEvidence:any[]=[];
 const branchEvaluationBudgetByFrontierTask:Record<string,number>={}; const frontierExhaustionReasonCounts:Record<string,number>={}; const partialPlanExhaustionReasonCounts:Record<string,number>={};
 const local=new Set<string>(); const attemptedExecutionTaskIds=new Set<number>(); const combinedAssignmentsFingerprints=new Set<string>(); const branchAssignmentsFingerprints=new Set<string>(); let ordinal=args.createdOrdinal, searchSpacesBuilt=0, materializationAttempts=0, transformationsExecuted=0, simulationsExecuted=0, validationsExecuted=0;
 let criticalChainWideningRoundCount=0, frontierTaskWideningRoundCount=0, criticalChainRanksScanned=0, maxCriticalChainRankScanned=0, frontierTasksAvailable=portfolio.uniqueExecutableFrontierTaskCount, frontierTasksScanned=0, maxFrontierTaskRankScanned=0, branchEvaluationCount=0, retainedValidBranchCount=0, rawInvalidBranchCount=0, rawClosureIncompleteBranchCount=0;
 let anchorPlacementRejectedBranchCount=0, closureSearchFailedBranchCount=0, closureAssignmentIntegrityRejectedCount=0, dependencyPrecheckRejectedBranchCount=0, hardValidationRejectedBranchCount=0, unsupportedBranchCount=0, repeatedFrontierMaterializationCount=0, avoidedDuplicateFrontierMaterializationCount=0, duplicateCombinedChildPrePipelinePruneCount=0;
 const selectedPrimaryGoalTaskIds:number[]=[]; const selectedSupportedGoalTaskIds:number[][]=[]; const supportedGoalsByMaterializedFrontierSample:any[]=[];
 const reopen=args.causalReopenContext??null; const excludedBranches=new Set([...(reopen?.excludedDecisionBranchFingerprints??[])].map(String)); const excludedAssignments=new Set([...(reopen?.excludedAssignmentsFingerprints??[])].map(String)); const previouslyEvaluated=new Set([...(reopen?.previouslyEvaluatedTemporalCandidateFingerprints??[])].map(String)); const orderedPortfolioCandidates=reopen?[...portfolio.candidates].sort((a:any,b:any)=>(Number(b.executionTaskId===reopen.targetExecutionTaskId)-Number(a.executionTaskId===reopen.targetExecutionTaskId))||(Number(b.primaryGoalTaskId===reopen.targetPrimaryGoalTaskId)-Number(a.primaryGoalTaskId===reopen.targetPrimaryGoalTaskId))):portfolio.candidates; const candidates=orderedPortfolioCandidates.slice(0,frontierLimit); let causalCheckpointExcludedBranchCount=0,causalCheckpointExcludedAssignmentCount=0,causalCheckpointPreviouslyEvaluatedCandidateSkipCount=0,causalCheckpointNewCandidateEvaluationCount=0; criticalChainRanksScanned=Math.min(allChains.length, Math.max(0,...candidates.flatMap((c:any)=>c.supportingChainRanks??[0]))); maxCriticalChainRankScanned=criticalChainRanksScanned;
 for(let frontierStart=0; frontierStart<candidates.length && children.length<args.budget.maxChildrenPerDecision; frontierStart+=frontierBatch){
  frontierTaskWideningRoundCount++;
  for(const pc of candidates.slice(frontierStart,frontierStart+frontierBatch)){
   const executionTaskId=Number(pc.executionTaskId); if(attemptedExecutionTaskIds.has(executionTaskId)){repeatedFrontierMaterializationCount++; avoidedDuplicateFrontierMaterializationCount+=Math.max(1,Number(pc.supportingChainCount??1)-1); continue;} attemptedExecutionTaskIds.add(executionTaskId);
   frontierTasksScanned++; maxFrontierTaskRankScanned=Math.max(maxFrontierTaskRankScanned,frontierStart+1); branchEvaluationBudgetByFrontierTask[String(executionTaskId)]=explorationLimit;
   const chain=(allChains??[]).find((c:any)=>Number(c.goalTaskId)===Number(pc.primaryGoalTaskId) && (c.executableFrontierTaskIds??[]).map(Number).includes(executionTaskId))??allChains[0]??{};
   const full=[...(chain.topologicalPendingChainTaskIds??[])].map(Number); const minimal=[executionTaskId]; const fullMaterialized=full.length===1&&full[0]===executionTaskId;
   frontierAudits.push({goalTaskId:pc.primaryGoalTaskId,primaryGoalTaskId:pc.primaryGoalTaskId,supportedGoalTaskIds:pc.supportedGoalTaskIds,supportingChainFingerprints:pc.supportingChainFingerprints,sharedCriticalFrontier:pc.supportedGoalTaskIds.length>1,executionTaskId,frontierTaskRank:frontierStart+1,fullGoalPendingClosureTaskIds:full,minimalExecutionClosureTaskIds:minimal,fullGoalClosureMaterialized:fullMaterialized,executionClosureContractValid:full.includes(executionTaskId)||full.length===0,inheritedCriticalitySourceTaskIds:pc.inheritedCriticalitySourceTaskIds});
   const anchorBase=anchorsByTaskId.get(executionTaskId);
   if(!anchorBase){frontierAudits.push({goalTaskId:pc.primaryGoalTaskId,executionTaskId,code:"FRONTIER_TASK_WITHOUT_CANONICAL_ANCHOR",supportedGoalTaskIds:pc.supportedGoalTaskIds}); frontierExhaustionReasonCounts.FRONTIER_TASK_WITHOUT_CANONICAL_ANCHOR=(frontierExhaustionReasonCounts.FRONTIER_TASK_WITHOUT_CANONICAL_ANCHOR??0)+1; continue;}
   const anchor={...anchorBase,primarySupportedGoalTaskId:pc.primaryGoalTaskId,supportedGoalTaskIds:pc.supportedGoalTaskIds,inheritedCriticalitySourceTaskIds:pc.inheritedCriticalitySourceTaskIds,sharedCriticalFrontier:pc.supportedGoalTaskIds.length>1,supportingChainFingerprints:pc.supportingChainFingerprints};
   const spaces=buildInitialConstructionSearchSpaces({input:args.originInput,anchors:[anchor] as any,initialConstructionMap:map,maxSearchSpaces:1,maxWindowsPerAnchor:20}); searchSpacesBuilt+=spaces.length;
   const stage={...args.stage1,selectedAnchor:anchor,selectedAnchorTaskId:executionTaskId,initialConstructionMap:map,searchSpaces:spaces};
   const attempt=materializeInitialConstructionAnchorAttempt({originInput:args.originInput,originOperationalState:args.originOperationalState,stage,anchor,baseProvisionalAssignments:parentAssignments,provisionallySatisfiedTaskIds:parentAssignments.map(a=>a.taskId),closureTaskIds:minimal,maxBranches:explorationLimit,reasoningBudget:args.reasoningBudget,createdAt:args.createdAt??null,canonicalContext:args.canonicalContext});
   materializationAttempts++; avoidedDuplicateFrontierMaterializationCount+=Math.max(0,Number(pc.supportingChainCount??1)-1); selectedPrimaryGoalTaskIds.push(pc.primaryGoalTaskId); selectedSupportedGoalTaskIds.push([...(pc.supportedGoalTaskIds as any)]); supportedGoalsByMaterializedFrontierSample.push({executionTaskId,primaryGoalTaskId:pc.primaryGoalTaskId,supportedGoalTaskIds:pc.supportedGoalTaskIds}); branchEvaluationCount+=Number(attempt.diagnostics?.branchEvaluationCount??attempt.attemptedBranchCount??0); rawInvalidBranchCount+=(attempt.attempts??[]).filter((a:any)=>a.rejectionReason==="hard-invalid"||a.validation?.result==="INVALID").length; rawClosureIncompleteBranchCount+=(attempt.branches??[]).filter((b:any)=>b.status==="closure-incomplete").length;
   anchorPlacementRejectedBranchCount+=Number(attempt.diagnostics?.anchorPlacementRejectedBranchCount??0); closureSearchFailedBranchCount+=Number(attempt.diagnostics?.closureSearchFailedBranchCount??0); closureAssignmentIntegrityRejectedCount+=Number(attempt.diagnostics?.closureAssignmentIntegrityRejectedCount??0); dependencyPrecheckRejectedBranchCount+=Number(attempt.diagnostics?.dependencyPrecheckRejectedBranchCount??attempt.diagnostics?.combinedDependencyPrecheckRejectedCount??0); hardValidationRejectedBranchCount+=Number(attempt.diagnostics?.hardValidationRejectedBranchCount??0); unsupportedBranchCount+=Number(attempt.diagnostics?.unsupportedBranchCount??0);
   const selectable=[...(attempt.selectable??[])].sort((a:any,b:any)=>String(a.branch.branchId).localeCompare(String(b.branch.branchId))); const seenOption=new Set<string>(); let retainedForFrontier=0;
   for(const option of selectable){
    const branchFp=option.branch.branchFingerprint??option.branch.branchId??digest(option.branch.assignments); if(excludedBranches.has(String(branchFp))){causalCheckpointExcludedBranchCount++; continue;} if(previouslyEvaluated.has(String(branchFp))){causalCheckpointPreviouslyEvaluatedCandidateSkipCount++; continue;} causalCheckpointNewCandidateEvaluationCount++; const branchAssignFp=initialConstructionAssignmentFingerprint(option.branch.assignments.map(normalize)); if(excludedAssignments.has(branchAssignFp)){causalCheckpointExcludedAssignmentCount++; continue;} if(branchAssignmentsFingerprints.has(branchAssignFp)){duplicates.push(branchAssignFp); continue;} branchAssignmentsFingerprints.add(branchAssignFp);
    const byId=new Map(parentAssignments.map(a=>[a.taskId,a])); for(const a of option.branch.assignments.map(normalize)) if(!byId.has(a.taskId)) byId.set(a.taskId,a);
    const combined=[...byId.values()].sort((a,b)=>a.taskId-b.taskId); const fp=initialConstructionAssignmentFingerprint(combined);
    if(excludedAssignments.has(fp)){causalCheckpointExcludedAssignmentCount++; continue;} if(combinedAssignmentsFingerprints.has(fp)||seenOption.has(fp)||combined.length===parentAssignments.length||local.has(fp)||args.caches?.seenAssignmentsFingerprints?.has(fp)){duplicates.push(fp); duplicateCombinedChildPrePipelinePruneCount++; continue;} combinedAssignmentsFingerprints.add(fp); seenOption.add(fp); local.add(fp);
    const tr=buildCandidateStates(args.originOperationalState,[candidate(`retained:${ordinal}`,combined)],{createdAt:args.createdAt??null,maxTransformations:1}); transformationsExecuted+=tr.candidateStates.length; const sim=simulateCandidateStates(args.originOperationalState,tr.candidateStates,{createdAt:args.createdAt??null,maxSimulations:1}); simulationsExecuted+=sim.simulatedStates.length; const validationResult=validateSimulatedStates(sim.simulatedStates,{createdAt:args.createdAt??null}); validationsExecuted+=validationResult.validationResults.length; const validation=validationResult.validationResults[0];
    if(validation?.result!=="VALID"){hardInvalid.push({assignmentsFingerprint:fp,validationResult:validation?.result??null});continue;}
    const childMap=buildInitialConstructionMap({input:args.originInput,state:args.originOperationalState,planningMode:"INITIAL_CONSTRUCTION",provisionalAssignments:combined,provisionallyAssignedTaskIds:combined.map(a=>a.taskId)});
    const childResidual=buildInitialConstructionResidualContext({originInput:args.originInput,originOperationalState:args.originOperationalState,stage2:{...pseudo,selectedAssignments:combined}});
    const future=evaluateInitialConstructionPartialPlanFutureFeasibility({criticalChains:childMap.criticalChains??[],residualProductiveTaskCount:childResidual.residualProductiveTaskIds.length});
    const decision=`${pc.primaryGoalTaskId}:${executionTaskId}:${branchFp}`;
    const parentByTask=new Map(parentAssignments.map(a=>[a.taskId,digest(a)])); const introducedAssignments=combined.filter(a=>parentByTask.get(a.taskId)!==digest(a)).map(a=>({taskId:a.taskId,assignmentFingerprint:digest(a),previousAssignmentFingerprint:parentByTask.get(a.taskId)??null,readOnly:true})).sort((a,b)=>a.taskId-b.taskId); const lineagePayload={decisionId:decision,decisionDepth:args.parentPartialPlan.depth+1,primaryGoalTaskId:Number(pc.primaryGoalTaskId),executionTaskId,branchFingerprint:String(branchFp),introducedAssignments,parentAssignmentsFingerprint:args.parentPartialPlan.assignmentsFingerprint,resultingAssignmentsFingerprint:fp}; const lineageEntry={...lineagePayload,fingerprint:digest(lineagePayload),readOnly:true};
    const child={partialPlanId:`critical-chain:${ordinal}:${fp.slice(0,12)}`,parentPartialPlanId:args.parentPartialPlan.partialPlanId,depth:args.parentPartialPlan.depth+1,assignments:combined,assignmentsFingerprint:fp,goalTaskId:Number(pc.primaryGoalTaskId),primaryGoalTaskId:Number(pc.primaryGoalTaskId),supportedGoalTaskIds:pc.supportedGoalTaskIds,supportingChainFingerprints:pc.supportingChainFingerprints,sharedCriticalFrontier:pc.supportedGoalTaskIds.length>1,executionTaskId,executedFrontierTaskIds:[executionTaskId],minimalExecutionClosureTaskIds:minimal,decisionBranchFingerprint:String(branchFp),decisionPath:[...args.parentPartialPlan.decisionPath,decision],decisionLineage:[...((args.parentPartialPlan as any).decisionLineage??[]),lineageEntry],decisionLineageFingerprint:digest([...((args.parentPartialPlan as any).decisionLineage??[]).map((x:any)=>x.fingerprint),lineageEntry.fingerprint]),rootAssignmentTaskIds:(args.parentPartialPlan as any).rootAssignmentTaskIds??[],criticalChainMapFingerprint:childMap.criticalChainMapFingerprint,anchorRankingFingerprint:digest((childMap.criticalChains??[]).map((c:any)=>c.goalTaskId)),futureFeasibility:future,status:future.status==="INFEASIBLE"?"PRUNED":"SUSPENDED",createdOrdinal:ordinal++,readOnly:true};
    retainedForFrontier++; retainedValidBranchCount++; if(future.status==="INFEASIBLE") futureInfeasible.push(child); else children.push(child);
    if(retainedForFrontier>=retainLimit||children.length>=args.budget.maxChildrenPerDecision) break;
   }
   if(retainedForFrontier===0){ frontierExhaustionReasonCounts.NO_RETAINED_VALID_BRANCH=(frontierExhaustionReasonCounts.NO_RETAINED_VALID_BRANCH??0)+1; frontierFailureEvidence.push(buildInitialConstructionFrontierFailureEvidence({executionTaskId,primaryGoalTaskId:pc.primaryGoalTaskId,supportedGoalTaskIds:pc.supportedGoalTaskIds,retainedValidBranchCount:retainedForFrontier,rejectionReasonCodes:["NO_RETAINED_VALID_BRANCH"],diagnostics:attempt.diagnostics})); }
   if(children.length>=args.budget.maxChildrenPerDecision) break;
  }
 }
 const chainBatch=1, chainLimit=allChains.length; criticalChainWideningRoundCount=criticalChainRanksScanned?1:0;
 const allEligibleCriticalChainsExhausted=criticalChainRanksScanned>=allChains.length||criticalChainRanksScanned>=chainLimit;
 const allEligibleFrontierTasksExhausted=frontierTasksScanned>=Math.min(frontierTasksAvailable,criticalChainRanksScanned*frontierLimit);
 const allConfiguredBranchEvaluationsExhausted=branchEvaluationCount>=Object.keys(branchEvaluationBudgetByFrontierTask).length*explorationLimit;
 const falseDeadEndCount=0;
 const maximumSupportedGoalCountForFrontier=Math.max(0,...portfolio.candidates.map((c:any)=>c.supportedGoalTaskIds.length)); const averageSupportedGoalCountForFrontier=portfolio.candidates.length?portfolio.candidates.reduce((sum:any,c:any)=>sum+c.supportedGoalTaskIds.length,0)/portfolio.candidates.length:0;
 const raw={frontierPortfolio:portfolio,frontierPortfolioFingerprint:portfolio.portfolioFingerprint,chainFrontierOccurrenceCount:portfolio.chainFrontierOccurrenceCount,uniqueExecutableFrontierTaskCount:portfolio.uniqueExecutableFrontierTaskCount,duplicateChainFrontierOccurrenceCount:portfolio.duplicateChainFrontierOccurrenceCount,sharedExecutableFrontierTaskCount:portfolio.sharedExecutableFrontierTaskCount,maximumSupportedGoalCountForFrontier,averageSupportedGoalCountForFrontier,frontierPortfolioBuildCount:1,frontierPortfolioChangedCount:1,uniqueFrontierTasksScanned:frontierTasksScanned,materializedUniqueFrontierTaskCount:materializationAttempts,repeatedFrontierMaterializationCount,avoidedDuplicateFrontierMaterializationCount,duplicateCombinedChildPrePipelinePruneCount,childMapBuildCount:children.length+futureInfeasible.length,supportedGoalsByMaterializedFrontierSample,selectedPrimaryGoalTaskIds,selectedSupportedGoalTaskIds,branchEvaluationsPerSelectedTargetAssignment:children.length?branchEvaluationCount/children.length:branchEvaluationCount,materializationAttemptsPerSelectedTargetAssignment:children.length?materializationAttempts/children.length:materializationAttempts,anchorPlacementRejectedBranchCount,closureSearchFailedBranchCount,closureAssignmentIntegrityRejectedCount,dependencyPrecheckRejectedBranchCount,hardValidationRejectedBranchCount,unsupportedBranchCount,parentPartialPlanId:args.parentPartialPlan.partialPlanId,parentAssignmentsFingerprint:args.parentPartialPlan.assignmentsFingerprint,residualContext,initialConstructionMap:map,canonicalAnchors,anchorRanking:canonicalAnchors.map((a:any)=>a.anchorTaskId),chainsConsidered:allChains.slice(0,criticalChainRanksScanned),frontierTasksConsidered:frontierAudits,frontierFailureEvidence,hardValidChildren:children,hardInvalidChildren:hardInvalid,futureInfeasibleChildren:futureInfeasible,duplicateAssignmentsFingerprints:duplicates,transformationsExecuted,simulationsExecuted,validationsExecuted,searchSpacesBuilt,materializationAttempts,hardValidChildCount:children.length,hardInvalidChildCount:hardInvalid.length,futureInfeasibleChildCount:futureInfeasible.length,duplicateChildCount:duplicates.length,criticalChainWideningRoundCount,criticalChainRanksAvailable:allChains.length,criticalChainRanksScanned,maxCriticalChainRankScanned,frontierTaskWideningRoundCount,frontierTasksAvailable,frontierTasksScanned,maxFrontierTaskRankScanned,branchEvaluationCount,branchEvaluationBudgetByFrontierTask,retainedValidBranchCount,rawInvalidBranchCount,rawClosureIncompleteBranchCount,frontierExhaustionReasonCounts,partialPlanExhaustionReasonCounts,falseDeadEndCount,allEligibleCriticalChainsExhausted,allEligibleFrontierTasksExhausted,allConfiguredBranchEvaluationsExhausted,diagnostics:{canonicalExecutionAnchorCount:canonicalAnchors.length,canonicalTargetAnchorCount:canonicalAnchors.filter((a:any)=>a.countsAsConstructiveTarget).length,canonicalSupportAnchorCount:canonicalAnchors.filter((a:any)=>a.constructiveSupport).length,frontierTaskWithoutCanonicalAnchorCount:frontierAudits.filter((a:any)=>a.code==="FRONTIER_TASK_WITHOUT_CANONICAL_ANCHOR").length,frontierTaskWithoutCanonicalAnchorIds:[...new Set(frontierAudits.filter((a:any)=>a.code==="FRONTIER_TASK_WITHOUT_CANONICAL_ANCHOR").map((a:any)=>a.executionTaskId))],frontierTaskWithoutCanonicalAnchorReasonCounts:{FRONTIER_TASK_WITHOUT_CANONICAL_ANCHOR:frontierAudits.filter((a:any)=>a.code==="FRONTIER_TASK_WITHOUT_CANONICAL_ANCHOR").length},arrivalFrontierSelectionCount:frontierAudits.filter((a:any)=>map.classification.transportArrivalTasks?.some((t:any)=>t.id===a.executionTaskId)).length,arrivalFrontierMaterializationCount:frontierAudits.filter((a:any)=>map.classification.transportArrivalTasks?.some((t:any)=>t.id===a.executionTaskId)&&!a.code).length,supportFrontierSelectionCount:frontierAudits.filter((a:any)=>map.classification.constructiveSupportTasks?.some((t:any)=>t.id===a.executionTaskId)).length,supportFrontierMaterializationCount:frontierAudits.filter((a:any)=>map.classification.constructiveSupportTasks?.some((t:any)=>t.id===a.executionTaskId)&&!a.code).length,mapBuildCount:1+children.length+futureInfeasible.length,futureFeasibilityEvaluationCount:children.length+futureInfeasible.length,chainFrontierOccurrenceCount:portfolio.chainFrontierOccurrenceCount,uniqueExecutableFrontierTaskCount:portfolio.uniqueExecutableFrontierTaskCount,duplicateChainFrontierOccurrenceCount:portfolio.duplicateChainFrontierOccurrenceCount,sharedExecutableFrontierTaskCount:portfolio.sharedExecutableFrontierTaskCount,maximumSupportedGoalCountForFrontier,averageSupportedGoalCountForFrontier,frontierPortfolioBuildCount:1,frontierPortfolioFingerprint:portfolio.portfolioFingerprint,frontierPortfolioChangedCount:1,uniqueFrontierTasksScanned:frontierTasksScanned,materializedUniqueFrontierTaskCount:materializationAttempts,repeatedFrontierMaterializationCount,avoidedDuplicateFrontierMaterializationCount,duplicateCombinedChildPrePipelinePruneCount,childMapBuildCount:children.length+futureInfeasible.length,supportedGoalsByMaterializedFrontierSample,selectedPrimaryGoalTaskIds,selectedSupportedGoalTaskIds,branchEvaluationsPerSelectedTargetAssignment:children.length?branchEvaluationCount/children.length:branchEvaluationCount,materializationAttemptsPerSelectedTargetAssignment:children.length?materializationAttempts/children.length:materializationAttempts,anchorPlacementRejectedBranchCount,closureSearchFailedBranchCount,closureAssignmentIntegrityRejectedCount,dependencyPrecheckRejectedBranchCount,hardValidationRejectedBranchCount,unsupportedBranchCount},causalReopenContext:reopen,causalCheckpointExcludedBranchCount,causalCheckpointExcludedAssignmentCount,causalCheckpointPreviouslyEvaluatedCandidateSkipCount,causalCheckpointNewCandidateEvaluationCount,stopReason:children.length?"CHILDREN_GENERATED":"ALL_ELIGIBLE_FRONTIER_CANDIDATES_EXHAUSTED"};
 return deepFreeze({...raw,fingerprint:digest({parent:raw.parentAssignmentsFingerprint,children:children.map(c=>c.assignmentsFingerprint),invalid:hardInvalid,duplicates}),readOnly:true}) as any;
}
