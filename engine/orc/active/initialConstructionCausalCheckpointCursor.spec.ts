import test from "node:test";
import assert from "node:assert/strict";
import { buildInitialConstructionCausalCheckpointCursor, advanceInitialConstructionCausalCheckpointCursor, initialConstructionCausalCheckpointCursorKey } from "./initialConstructionCausalCheckpointCursor";

test("ID 321 cursor is built deterministically and advanced immutably",()=>{const key=initialConstructionCausalCheckpointCursorKey({conflictFingerprint:"c",checkpointFingerprint:"p",causalDecisionId:"d"}); const c=buildInitialConstructionCausalCheckpointCursor({conflictFingerprint:"c",checkpointFingerprint:"p",causalDecisionId:"d",buildOrdinal:1,observedDecisionBranchFingerprints:["b2","b1","b1"],observedChildAssignmentsFingerprints:["a1"]}); assert.equal(c.cursorKey,key); assert.deepEqual(c.observedDecisionBranchFingerprints,["b1","b2"]); const n=advanceInitialConstructionCausalCheckpointCursor(c,{inspectedDecisionBranchFingerprints:["b1"],activatedDecisionBranchFingerprints:["b1"],evaluatedTemporalCandidateFingerprints:["t1"],observedChildAssignmentsFingerprints:["a2"]}); assert.equal(c.advanceOrdinal,0); assert.equal(n.advanceOrdinal,1); assert.deepEqual(n.inspectedDecisionBranchFingerprints,["b1"]); assert.deepEqual(n.evaluatedTemporalCandidateFingerprints,["t1"]); assert.notEqual(n.fingerprint,c.fingerprint);});
