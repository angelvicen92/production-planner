import test from "node:test";
import assert from "node:assert/strict";
import type { EngineInput } from "../types";
import { inspectEngineInput } from "./engineInputPreflight";
const input = (): EngineInput => ({ planId:1, workDay:{start:"09:00",end:"18:00"}, meal:{start:"13:00",end:"14:00"}, camerasAvailable:1, tasks:[{id:1,planId:1,templateId:10,templateName:"A",status:"pending",contestantId:1,spaceId:1,zoneId:1,durationOverrideMin:10}], locks:[], zoneResourceAssignments:{}, spaceResourceAssignments:{}, zoneResourceTypeRequirements:{}, spaceResourceTypeRequirements:{}, planResourceItems:[{id:5,resourceItemId:5,typeId:1,name:"R",isAvailable:true}], resourceItemComponents:{}, groupingZoneIds:[], contestantAvailabilityById:{1:{start:"09:00",end:"18:00"}}, spaceNameById:{1:"S"}, taskTemplateNameById:{10:"A"} });
test("preflight valid input",()=>{ const r=inspectEngineInput(input()); assert.equal(r.valid,true); assert.equal(r.facts.contestants,1); });
test("missing dependency, self dependency, and cycle are errors",()=>{ const i=input(); i.tasks.push({id:2,planId:1,templateId:10,status:"pending",contestantId:1,spaceId:1,zoneId:1,dependsOnTaskIds:[999,2],durationOverrideMin:10},{id:3,planId:1,templateId:10,status:"pending",contestantId:1,spaceId:1,zoneId:1,dependsOnTaskIds:[4],durationOverrideMin:10},{id:4,planId:1,templateId:10,status:"pending",contestantId:1,spaceId:1,zoneId:1,dependsOnTaskIds:[3],durationOverrideMin:10}); const codes=inspectEngineInput(i).errors.map(e=>e.code); assert.ok(codes.includes("DEPENDENCY_TASK_NOT_FOUND")); assert.ok(codes.includes("SELF_DEPENDENCY")); assert.ok(codes.includes("DEPENDENCY_CYCLE")); });
test("space and resource missing are errors",()=>{ const i=input(); i.tasks[0].spaceId=99; i.tasks[0].assignedResourceIds=[42]; const codes=inspectEngineInput(i).errors.map(e=>e.code); assert.ok(codes.includes("SPACE_NOT_FOUND")); assert.ok(codes.includes("RESOURCE_NOT_FOUND")); });
test("configuration contradictions are warnings and unchanged",()=>{ const i=input(); i.optimizerMainZoneId=1; i.optimizerPrioritizeMainZone=false; i.optimizerMainZonePriorityLevel=0; i.optimizerMainZoneOptKeepBusy=true; i.optimizerMainZoneOptFinishEarly=false; i.optimizerContestantCompactLevel=0; i.optimizerContestantStayInZoneLevel=0; i.optimizerGroupBySpaceAndTemplate=true; i.optimizerGroupingLevel=0; const r=inspectEngineInput(i); assert.equal(r.valid,true); assert.equal(r.configuration.mainZoneId,1); assert.equal(r.configuration.prioritizeMainZone,false); assert.ok(r.warnings.some(w=>w.code==="MAIN_ZONE_IDENTIFIED_BUT_NOT_PRIORITIZED")); assert.ok(r.warnings.some(w=>w.code==="GROUPING_LEVEL_ZERO_WITH_GROUPING_FLAG_ENABLED")); });
test("meal placeholders without contestant are synthetic and not contestants",()=>{ const i=input(); i.tasks = [...Array.from({length:19},(_,idx)=>({id:idx+1,planId:1,templateId:10,status:"pending" as const,contestantId:idx+1,spaceId:1,zoneId:1,durationOverrideMin:10})), ...Array.from({length:26},(_,idx)=>({id:100+idx,planId:1,templateId:99,templateName:"Comida",status:"pending" as const,contestantId:null,spaceId:1,zoneId:1,breakKind:"space_meal",operationalRole:"meal_break_placeholder" as const,durationOverrideMin:30}))]; i.contestantAvailabilityById=Object.fromEntries(Array.from({length:19},(_,idx)=>[idx+1,{start:"09:00",end:"18:00"}])); const r=inspectEngineInput(i); assert.equal(r.facts.contestants,19); assert.equal(r.facts.syntheticTasks,26); assert.equal(r.facts.meal.syntheticSpaceMealTasks,26); });
