import assert from "node:assert/strict";import test from "node:test";import {existsSync,readFileSync} from "node:fs";import {buildRealityArtifact,digest,historicalSource} from "./runPlannerNextFocalA2RealityBaselineBenchmark";
test("runner derives honest acceptance and preserves the historical scenarios",()=>{const path=existsSync("planner-next-focal-a2-band-semantics-v4.json")?"planner-next-focal-a2-band-semantics-v4.json":"planner-next-focal-a2-reality-baseline-v1.json";const source=historicalSource(JSON.parse(readFileSync(path,"utf8"))),manifest=JSON.parse(readFileSync("engine/planner-next/benchmarks/focal-a2/focalA2RealityBaselineHistoricalManifest.json","utf8"));const before=digest(source),out=buildRealityArtifact(source,manifest);assert.equal(digest(source),before);assert.equal(Object.keys(out.scenarios).length,27);assert.equal(out.acceptance.accepted,true);assert.equal(out.acceptance.fullRealityBenchmarkPassed,out.currentRealityRun.complete&&out.acceptance.currentPlannerRealityHardValid);assert.equal(out.impossibleRealityRun.arraysEmpty,true);assert.equal(out.historicalRegressionEvidence.intact,true)});
