import crypto from "node:crypto";import fs from "node:fs";import {pathToFileURL} from "node:url";
import {planMainFlowAndFeeders} from "../planMainFlowAndFeeders";import {projectCombinedFocalA2ItinerantProblem} from "./focal-a2/focalA2RealityReference";
const OUTPUT="planner-next-focal-a2-itinerant-spec08-foundation-v4.json";const sourceUrl=new URL("./focal-a2/focalA2Spec08FoundationV3AcceptedArtifact.json",import.meta.url);const sourceBytes=fs.readFileSync(sourceUrl);const source=JSON.parse(sourceBytes.toString());const sourceSha256=crypto.createHash("sha256").update(sourceBytes).digest("hex");
const canonical=(v:any):any=>Array.isArray(v)?v.map(canonical):v&&typeof v==="object"?Object.fromEntries(Object.keys(v).filter(k=>k!=="runtimeMs").sort().map(k=>[k,canonical(v[k])])):v;const digest=(v:any)=>crypto.createHash("sha256").update(JSON.stringify(canonical(v))).digest("hex");
export function runSpec08FoundationV4Benchmark(){const problem=projectCombinedFocalA2ItinerantProblem(),before=JSON.stringify(problem),result=planMainFlowAndFeeders(problem),repeat=planMainFlowAndFeeders(projectCombinedFocalA2ItinerantProblem()),reversedProblem=projectCombinedFocalA2ItinerantProblem();reversedProblem.tasks.reverse();reversedProblem.anchoredAccompaniments!.reverse();const reversed=planMainFlowAndFeeders(reversedProblem);const reality=result.scheduledTasks.filter(t=>t.id.startsWith("reality-operation-")),anchors=new Set(problem.anchoredAccompaniments!.map(c=>c.anchorTaskId));const mainAnchors=result.scheduledTasks.filter(t=>anchors.has(t.id));const checks={complete:result.complete,hardValid:result.metrics.hardValid,taskCount:result.scheduledTasks.length===53,mainCount:result.scheduledTasks.filter(t=>t.kind==="main").length===19,vocalCount:result.scheduledTasks.filter(t=>t.kind==="vocal").length===19,anchoredCount:result.metrics.anchoredAccompanimentPlannedCount===3,segmentCount:result.metrics.anchoredAccompanimentScheduledSegmentCount===6,itinerantOperationCount:reality.length===15&&result.metrics.anchoredAccompanimentPlannedCount+9===12,productiveMinutes:reality.reduce((n,t)=>n+t.duration,0)+mainAnchors.reduce((n,t)=>n+t.duration,0)===375,anchorDuration:mainAnchors.every(t=>t.duration===15),deterministic:result.metrics.planFingerprint===repeat.metrics.planFingerprint,orderInvariant:result.metrics.planFingerprint===reversed.metrics.planFingerprint,inputImmutable:JSON.stringify(problem)===before,budgetStable:result.metrics.branchesExplored<=problem.budget.maxBranchExpansions,outputUnread:!fs.existsSync(OUTPUT)};const accepted=Object.values(checks).every(Boolean);const active={checks,complete:result.complete,hardValid:result.metrics.hardValid,plannedTaskCount:result.scheduledTasks.length,planFingerprint:result.metrics.planFingerprint,branchesExplored:result.metrics.branchesExplored,maxBranchExpansions:problem.budget.maxBranchExpansions,metrics:result.metrics,tasks:result.scheduledTasks.map(t=>({id:t.id,kind:t.kind,start:t.start,end:t.end,duration:t.duration,spaceId:t.spaceId,requiredResourceIds:t.requiredResourceIds??[]})),humanComparisonInformationalOnly:true,humanScheduleUsedAsSeed:false};const artifact={version:"planner-next-focal-a2-itinerant-spec08-foundation-v4",status:accepted?"FOCAL_A2_SPEC08_MAIN_ANCHORED_ACCOMPANIMENT_ACCEPTED":"FOCAL_A2_SPEC08_VALIDATION_REQUIRED",sourceArtifactVersion:source.version,sourceArtifactSha256:sourceSha256,scenarioCount:33,scenarios:{...source.scenarios,focalA2Spec08MainAnchoredAccompaniment:active},activeScenarioId:"focalA2Spec08MainAnchoredAccompaniment",historicalScenarioDigests:Object.fromEntries(Object.entries(source.scenarios).map(([id,v])=>[id,digest(v)])),acceptance:{accepted,mainAnchoredAccompanimentSupported:accepted,nonMainAnchorsSupported:false,plannerNextProductionIntegrated:false},artifactFingerprint:digest(active)};process.stdout.write(JSON.stringify(artifact,null,2)+"\n");return artifact}
if(import.meta.url===pathToFileURL(process.argv[1]??"").href)runSpec08FoundationV4Benchmark();
