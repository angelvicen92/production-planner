import assert from "node:assert/strict";import test from "node:test";import {readFileSync} from "node:fs";import {runSpec10015Benchmark} from "./runPlannerNextItinerantUnitMealsBenchmark";
test("SPEC10-015 benchmark is deterministic and derived from both policies",()=>{runSpec10015Benchmark();const first=runSpec10015Benchmark(),second=runSpec10015Benchmark();assert.deepEqual(first,second);assert.deepEqual(first.map(x=>x.policy),["COMPATIBILITY_PRESERVING","EXACT_CONSTRUCTIVE"]);assert.ok(first.every(x=>x.complete&&x.hardValid&&x.inputImmutable&&x.fictitiousResourceAbsent&&x.overlappingCandidateRejected&&x.anchored.overlapRejected&&x.anchored.alternativeAccepted&&x.unit8DuringMeal.length===1));const fixture=JSON.parse(readFileSync("engine/planner-next/benchmarks/fixtures/spec10-015-itinerant-unit-meals-evidence.json","utf8"));assert.deepEqual(fixture.executions,first);assert.equal(fixture.deterministic,true);});
