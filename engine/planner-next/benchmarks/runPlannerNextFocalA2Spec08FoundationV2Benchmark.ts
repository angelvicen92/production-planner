import fs from "node:fs";import {pathToFileURL} from "node:url";import {planMainFlowAndFeeders} from "../planMainFlowAndFeeders";import {projectStandaloneFocalA2RealityProblem,projectCombinedFocalA2ItinerantProblem} from "./focal-a2/focalA2RealityReference";
export function runSpec08FoundationV2Benchmark(){const artifact=JSON.parse(fs.readFileSync("planner-next-focal-a2-itinerant-spec08-foundation-v2.json","utf8"));const standalone=planMainFlowAndFeeders(projectStandaloneFocalA2RealityProblem());const combined=planMainFlowAndFeeders(projectCombinedFocalA2ItinerantProblem());if(!standalone.complete||!standalone.metrics.hardValid||standalone.scheduledTasks.length!==47)throw new Error("standalone evidence failed");if(combined.complete||combined.metrics.branchesExplored!==0||combined.scheduledTasks.length||combined.metrics.reasonCodes.join()!=="ANCHORED_ACCOMPANIMENT_NOT_SUPPORTED")throw new Error("combined atomic evidence failed");process.stdout.write(JSON.stringify(artifact,null,2)+"\n");return artifact}if(import.meta.url===pathToFileURL(process.argv[1]??"").href)runSpec08FoundationV2Benchmark();
