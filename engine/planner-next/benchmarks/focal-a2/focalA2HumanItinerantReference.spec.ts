import assert from "node:assert/strict";import test from "node:test";import {focalA2HumanItinerantReference} from "./focalA2HumanItinerantReference";import {projectStandaloneFocalA2RealityProblem} from "./focalA2RealityReference";
test("poisoning external human times cannot affect operational projection",()=>{const before=projectStandaloneFocalA2RealityProblem();const poison=[...structuredClone(focalA2HumanItinerantReference)].reverse().map((x,i)=>({...x,start:i,end:i+1,informationalLocation:"POISON"}));assert.notDeepEqual(poison,focalA2HumanItinerantReference);assert.deepEqual(projectStandaloneFocalA2RealityProblem(),before)});
