import assert from "node:assert/strict";import test from "node:test";import { focalA2ItinerantCompositionControls } from "./focalA2ItinerantCompositionControls";
test("SPEC-08 composition windows are independent and enforced",()=>{const x=focalA2ItinerantCompositionControls();assert.equal(x.exactCompositions,true);assert.equal(x.unitIdAbsentFromRequirements,true);assert.equal(x.combinedBefore960Rejected,true);assert.equal(x.combinedFrom960Accepted,true);assert.deepEqual(x.compositionWindows.map(u=>u.availability),[[{start:660,end:840}],[{start:675,end:810}],[{start:960,end:1080}]])});
