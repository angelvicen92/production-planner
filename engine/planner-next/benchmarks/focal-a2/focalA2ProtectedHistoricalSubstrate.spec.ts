import assert from "node:assert/strict";import test from "node:test";import {historicalDigest,inspectProtectedHistoricalSubstrate} from "./focalA2ProtectedHistoricalSubstrate";
test("historical substrate is pure and detects scenario/evidence poison",()=>{const artifact={scenarios:{a:{runtimeMs:1,value:2}},historicalEvidence:{e:{value:3}}};const before=JSON.stringify(artifact);const manifest={scenarioDigests:{a:historicalDigest(artifact.scenarios.a)},historicalEvidenceDigests:{e:historicalDigest(artifact.historicalEvidence.e)}};assert.equal(inspectProtectedHistoricalSubstrate(artifact,manifest).passed,true);artifact.scenarios.a.value=4;assert.deepEqual(inspectProtectedHistoricalSubstrate(artifact,manifest).scenarioMismatches,["a"]);assert.notEqual(JSON.stringify(artifact),before)});
