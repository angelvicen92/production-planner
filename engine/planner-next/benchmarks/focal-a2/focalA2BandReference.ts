import type { PlannerNextProblem } from "../../contracts";
import { focalA2Problem } from "./focalA2Problem";
import { focalA2Reference } from "./focalA2Reference";
import { evaluateContinuousResourcePresence,type PresenceConcentrationPolicy } from "./evaluateContinuousResourcePresence";
export type FocalA2ParticipantRequirementProfile={participantId:string;displayName:string;requiresBand:boolean;usesInstrument:boolean;sourceAnnotation:string|null;sourceConfidence:"EXPLICIT_MAIN_STAGE_ANNOTATION"|"USER_CONFIRMED_COMPLEMENT_OF_EXPLICIT_INSTRUMENT_SET"};
export type ProjectedMainTaskRequirements={participantId:string;requiredResourceIds:string[];instrumentSetupRequired:boolean};
export type ContinuousResourceReferencePolicy={resourceId:string;assignedSpaceId:string;availability:{start:number;end:number}[];authorizedMeal:{start:number;end:number};presenceConcentrationPolicy:PresenceConcentrationPolicy};
export type CurrentBandMode="CURRENT_OFF"|"CURRENT_MAXIMUM"|"CURRENT_PREFERRED";
const instruments:Record<string,string>={"moises-salazar-ramirez":"SOLO A PIANO","angel-gonzalez":"GUITARRA","julio-gomez":"GUITARRA","lina-isabel-garcia-salcedo":"GUITARRISTA FLAMENCO","marta-fonrali":"GUITARRA","pere-portero":"GUITARRA"};
export const focalA2ParticipantRequirementProfiles:FocalA2ParticipantRequirementProfile[]=focalA2Reference.participants.map(p=>{const annotation=instruments[p.participantId];return {participantId:p.participantId,displayName:p.displayName,requiresBand:!annotation,usesInstrument:Boolean(annotation),sourceAnnotation:annotation??null,sourceConfidence:annotation?"EXPLICIT_MAIN_STAGE_ANNOTATION":"USER_CONFIRMED_COMPLEMENT_OF_EXPLICIT_INSTRUMENT_SET"};});
export const FOCAL_A2_BAND_RESOURCE_ID="focal-a2-continuous-resource";
export const focalA2ContinuousResourcePolicy:ContinuousResourceReferencePolicy={resourceId:FOCAL_A2_BAND_RESOURCE_ID,assignedSpaceId:"main-stage",availability:[{start:675,end:1035}],authorizedMeal:{start:840,end:915},presenceConcentrationPolicy:"PREFERRED"};
export function projectRequirement(profile:Pick<FocalA2ParticipantRequirementProfile,"participantId"|"requiresBand"|"usesInstrument">,existing:string[]=[]):ProjectedMainTaskRequirements{return {participantId:profile.participantId,requiredResourceIds:profile.requiresBand?[...new Set([...existing,FOCAL_A2_BAND_RESOURCE_ID])]:[...existing],instrumentSetupRequired:profile.usesInstrument};}
export function projectFocalA2BandProblem(mode:CurrentBandMode):PlannerNextProblem{const p=focalA2Problem(),band=new Set(focalA2ParticipantRequirementProfiles.filter(x=>x.requiresBand).map(x=>x.participantId));return {...p,resources:[...p.resources,{id:FOCAL_A2_BAND_RESOURCE_ID,availability:[{start:675,end:1035}],presencePreference:mode==="CURRENT_OFF"?"OFF":"MAXIMUM",...(mode==="CURRENT_PREFERRED"?{presenceConcentrationPolicy:"PREFERRED" as const,assignedSpaceId:"main-stage"}:{})}],tasks:p.tasks.map(t=>t.kind==="main"&&band.has(t.participantId)?{...t,requiredResourceIds:[...new Set([...(t.requiredResourceIds??[]),FOCAL_A2_BAND_RESOURCE_ID])]}:{...t,requiredResourceIds:t.requiredResourceIds?[...t.requiredResourceIds]:undefined})};}
export const focalA2BandMeal={spaceId:"main-stage",assignedSpaceId:"main-stage",start:840,end:915};
export function referenceBandPresence(){const ids=new Set(focalA2ParticipantRequirementProfiles.filter(x=>x.requiresBand).map(x=>x.participantId));return evaluateContinuousResourcePresence(focalA2Reference.tasks.filter(t=>t.kind==="main"&&ids.has(t.participantId)),focalA2BandMeal,"PREFERRED",ids.size);}
export function scheduledBandPresence(tasks:Array<{id:string;participantId?:string;kind:string;start:number;end:number}>){const ids=new Set(focalA2ParticipantRequirementProfiles.filter(x=>x.requiresBand).map(x=>x.participantId));return evaluateContinuousResourcePresence(tasks.filter(t=>t.kind==="main"&&t.participantId&&ids.has(t.participantId)),focalA2BandMeal,"PREFERRED",ids.size);}
