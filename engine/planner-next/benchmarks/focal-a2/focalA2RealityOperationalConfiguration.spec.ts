import assert from "node:assert/strict";import test from "node:test";import {focalA2RealityAuxiliaryPolicy,focalA2RealityParticipantAvailabilityOverrides,participantAvailabilitySource,auxiliaryPolicyEvidence} from "./focalA2RealityOperationalConfiguration";
test("Reality operational configuration is explicit and order-neutral",()=>{assert.equal(focalA2RealityAuxiliaryPolicy.participantPresencePreference,"OFF");assert.equal(auxiliaryPolicyEvidence.fallbackUsed,false);assert.equal(participantAvailabilitySource.humanTaskTimesUsed,false);assert.deepEqual(focalA2RealityParticipantAvailabilityOverrides,{"lina-isabel-garcia-salcedo":[{start:570,end:1080}],"marta-fonrali":[{start:670,end:1080}],"linet-varela":[{start:600,end:1080}],"carmen-maria-saborido":[{start:570,end:1080}],"eva-martin-fernandez":[{start:630,end:1080}]});assert.ok(Object.values(focalA2RealityParticipantAvailabilityOverrides).every(w=>w.length===1))});
