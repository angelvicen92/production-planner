import assert from "node:assert/strict";import test from "node:test";import fs from "node:fs";import {inspectProtectedHistoricalSubstrate} from "./focal-a2/focalA2ProtectedHistoricalSubstrate";import manifest from "./focal-a2/focalA2Spec08FoundationV2HistoricalManifest.json" with {type:"json"};
const artifact=JSON.parse(fs.readFileSync("planner-next-focal-a2-itinerant-spec08-foundation-v2.json","utf8"));
test("v2 is self-contained and protects its thirty source scenarios",()=>{const x=inspectProtectedHistoricalSubstrate(artifact,manifest);assert.equal(x.passed,true);assert.equal(Object.keys(manifest.scenarioDigests).length,30);assert.equal(artifact.scenarioCount,31);assert.equal(manifest.activeScenarioId,"focalA2Spec08FoundationConsolidation");assert.equal(artifact.supersededHistoricalScenarios.focalA2Spec08FoundationRepair,"SUPERSEDED_UNDERIVED_ACCEPTANCE_AND_REFERENCE_TIME_COUPLING")});
test("v2 acceptance is derived and preserves the intentional gap",()=>{assert.ok(artifact.requiredPositiveChecks.length>20);assert.ok(artifact.requiredPositiveChecks.every((id:string)=>artifact.checks[id].passed));assert.equal(artifact.acceptance.anchoredAccompanimentSupported,false);assert.equal(artifact.acceptance.fullFocalA2ItinerantBenchmarkPassed,false);assert.equal(artifact.acceptance.accepted,true);assert.deepEqual(artifact.remainingGapCodes,["GENERIC_ANCHORED_ACCOMPANIMENT_NOT_SUPPORTED"])});
