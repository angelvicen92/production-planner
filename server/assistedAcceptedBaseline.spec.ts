import assert from "node:assert/strict";import test from "node:test";
import { acceptedRequiredViolations, affectedTasksUnchanged, resolveActiveStageLineage } from "./assistedAcceptedBaseline";
const task=(start:string)=>({contractVersion:1,tasks:[{taskId:1,startPlanned:start,endPlanned:"10:30",spaceId:2}]});
const required={ruleCode:"RESOURCE_REQUIRED_PRESENCE_VIOLATION",severity:"REQUIRED" as const,violationKey:"required:1",affectedTaskIds:[1],affectedResourceIds:[4],affectedSpaceIds:[2],details:{},inheritedAcceptedExceptionId:null};
test("active lineage excludes archived future and sibling branches",()=>{const stages:any[]=[{id:1,parentStageId:null,snapshotJson:task("10:00")},{id:2,parentStageId:1,snapshotJson:task("10:00")},{id:3,parentStageId:2,snapshotJson:task("10:00")},{id:4,parentStageId:2,archivedAt:"now",snapshotJson:task("10:00")}];assert.deepEqual(resolveActiveStageLineage(stages,3).map(x=>x.id),[3,2,1]);assert.ok(!resolveActiveStageLineage(stages,3).some(x=>x.id===4));});
test("accepted REQUIRED is inherited only while affected tasks are unchanged",()=>{const stage:any={id:2,parentStageId:1,snapshotJson:task("10:00"),validationSummaryJson:{report:{violations:[required]}}};assert.equal(acceptedRequiredViolations([stage],task("10:00")).length,1);assert.equal(acceptedRequiredViolations([stage],task("10:05")).length,0);assert.equal(affectedTasksUnchanged(stage.snapshotJson,task("10:00"),[1]),true);});

test("missing lineage parent fails closed",()=>assert.throws(()=>resolveActiveStageLineage([{id:3,parentStageId:2,snapshotJson:task("10:00")}],3),/CORRUPT_STAGE_LINEAGE/));
